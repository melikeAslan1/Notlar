create view structureView asSELECTst."CompanyId",sd."Id",sd."StructureTypeId",sd."StructureName"FROM   "StructuresDetails" as sdINNER JOIN "StructuresTypes" as st    ON sd."StructureTypeId"::text = st."Id"::textwhere sd."IsDeleted" = false and st."IsDeleted" = false;alter table structureView    owner to doadmin;grant delete, insert, references, select, trigger, truncate, update on structureView to structure_user;